spring:
  jpa:
    hibernate:
      ddl-auto: update #ทุกครั้งที่มีปิดโปรแกรม จะมีการอัพเดททุกครั้ง ถ้าข้น PRD อาจจะเปลี่ยนเป็น None
  datasource:
    url: jdbc:postgresql://localhost:5432/backend
    username: postgres
    password: syspass
  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 5MB



